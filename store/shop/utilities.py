# from django.core.paginator import Paginator


# def paginator(request, item, count=10):
#     paginator = Paginator(item, count)
#     page_number = request.GET.get("page")
#     return paginator.get_page(page_number)
